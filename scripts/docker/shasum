#!/bin/sh
set -eu

if [ "$#" -ge 2 ] && [ "$1" = "-a" ] && [ "$2" = "256" ]; then
  shift 2
  exec sha256sum "$@"
fi

printf '%s\n' 'This verification-image shasum compatibility command supports only: shasum -a 256 <file>' >&2
exit 64
